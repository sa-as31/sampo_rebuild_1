# 无人机图标接入记录（2026-03-18）

## 目标

将用户提供的 `无人机.png` 接入 Web 演示页面，用于表示智能体位置，替代原先的纯实心圆样式。

## 修改内容

### 1. 后端新增图标静态接口

修改文件：`web_demo/server.py`

- 新增 `GET /uav-icon.png` 路由
- 从仓库根目录读取 `无人机.png` 并返回给前端
- 若图标文件不存在，返回 404

### 2. 前端动画渲染改造

修改文件：`web_demo/static/app.js`

- 新增无人机图标加载逻辑（`Image` 对象）
- 智能体绘制从“实心圆”改为“图标优先 + 圆形回退”
- 加入图标缩放缓存（按像素尺寸缓存离屏 sprite），避免每帧重复高开销缩放
- 保留智能体编号显示，改为图标右下角编号徽标
- 保留不同智能体颜色区分（外圈描边颜色）

### 3. 页面文案同步

修改文件：`web_demo/static/index.html`

- 将“实心圆为无人机”调整为“无人机图标为智能体位置”

## 体积与性能说明

- 当前图标文件：`无人机.png`
- 尺寸：`1408 x 768`
- 文件大小：约 `557 KB`

前端通过“按单元格尺寸缩放 + sprite 缓存”策略降低了大图直接逐帧绘制的性能开销。若后续仍有性能压力，可再导出更小尺寸（如 `256x256` 或 `384x384`）版本替换。

## 2026-03-18 补充修复

用户反馈页面未看到无人机图标后，追加了以下修复：

1. 新增专用小图标：`web_demo/static/uav-icon-384.png`
2. 前端改为加载专用图标（带版本号参数）：
   - `app.js` 中 `agentIcon.image.src = "/uav-icon-384.png?v=20260318c"`
3. `index.html` 的脚本引用也增加版本号，避免浏览器继续使用旧缓存
4. 服务端增加 `Cache-Control: no-store`，减少静态资源缓存导致的“看不到新效果”问题
