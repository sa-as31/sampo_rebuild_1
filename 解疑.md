# 解疑

这个文档用于持续记录我在项目推进过程中提出的问题，以及当前已经确认的答案。

## 1. 论文中的“多智能体”指什么

- 这里的多智能体，指的是多架无人机。
- 每一架无人机都被看作一个独立智能体，拥有自己的位置、目标、局部观测和动作决策。
- 多智能体路径规划的难点不在单机找路，而在多机共享环境中的冲突规避与协同决策。

## 2. 页面里的“完成比例”是否合理

- 对终身任务来说，这个指标不合适。
- 因为智能体到达目标后不会退出，而是会被分配一个新目标继续执行任务。
- 更适合的指标是：
  - 累计完成任务数
  - 吞吐量

## 3. `max_frames=200` 是否表示每个智能体最多移动 200 步

- 更准确地说，`max_frames` 是整次 Web 推理回放的全局步数上限。
- 在同步多智能体环境里，可以近似理解为每个智能体最多参与 200 次动作决策时刻。
- 实际停止还会受到 `max_episode_steps` 和环境自身结束条件影响。

## 4. 智能体能否感知所有无遮挡区域

- 不能。
- 当前项目里，模型使用的是局部观测，不是“无遮挡全局视野”。
- 经过预处理后，模型主输入是以智能体为中心的 `11 x 11` 局部窗口。
- 因此，窗口外的环境信息对当前一步决策是不可见的。

## 5. 为什么终点就在旁边，智能体还可能在终点附近移动

- 这不一定意味着“没看见终点”。
- 更可能的原因包括：
  - 终身任务设定：到达目标后会立刻分配新目标
  - 多智能体冲突规避：当前一步不一定适合直接进入目标格
  - 奖励设计和子目标跟随机制：模型在局部上可能优先服从 planner 提供的引导
  - 训练效果仍可能不够理想，导致局部绕行或震荡

## 6. 模型在系统里起什么作用

- 模型负责给每个智能体输出下一步动作。
- 动作一般是离散动作，例如上、下、左、右、停留。
- 因此，运行时真正控制智能体移动的是模型的动作输出。

## 7. 智能体移动是否只靠模型

- 不是。
- 这个项目采用的是“规划算法 + 强化学习模型”的两阶段协同方式。
- 可以理解为：
  - A* / LB-A* 类规划器先生成基础路径或子目标引导
  - MARL 模型再结合局部观测和多机交互，对动作做决策精炼
- 因此不是纯模型端到端规划，而是“先规划、再精炼”的协同结构。

## 8. 当前 Web 原型后端是否已经支持终身任务指标

- 已开始改造。
- 当前页面的核心指标已改为：
  - 累计完成任务数
  - 吞吐量（任务数 / 步数）

## 9. 这个项目如何用两句话写进简历

- 可写成：负责基于 Sample Factory 与 Pogema 搭建多无人机协同路径规划与强化学习训练系统，结合传统规划器与多智能体强化学习模型，实现复杂栅格环境下的路径生成、冲突规避与策略训练。
- 可写成：独立完成项目的 CPU/GPU 训练链路、Docker 部署及 Web 推演原型开发，支持模型推理回放、关键指标展示与多智能体动态可视化。

## 10. 运营模式里为什么到终点后又播放了一遍动画

- 原因是前端播放逻辑在到达最后一帧时，会把帧索引重置为 `0`，属于循环播放设计。
- 已修复为：到达最后一帧后自动停止，不再回卷到开头，并将状态显示为“任务已完成”。
- 当前行为：
  - 点击“开始任务”从首帧播放到末帧后停止；
  - 点击“继续”时如果已经在末帧，会保持末帧并提示“任务已完成”。

## 11. 为什么运营模式里会看到无人机穿过灰色墙体

- 你的理解是正确的：灰色块代表障碍/墙体。
- 之前的原因是：运营模式轨迹由前端线性插值生成，没有逐步避障约束。
- 现已替换为栅格 A* 路径生成，样例轨迹会绕开灰色障碍块，不再穿墙。
- 当前状态是：
  - 目标点仍然在前端校验，障碍格不能被设为目标；
  - 轨迹生成从“直线插值动画”升级为“基于障碍地图的可通行路径”。

## 12. 三个任务模板是否有各自对应地图

- 已支持对应关系，不再共用同一障碍布局。
- 当前模板与地图为：
  - 仓储巡检 -> `warehouse-grid-v1`
  - 园区配送 -> `campus-road-v1`
  - 应急调度 -> `emergency-block-v1`
- 在运营模式切换模板时，会重建对应地图、起终点和 A* 轨迹，并从第 0 帧重新展示。

## 13. 未来 3D 模拟页面用哪种方式展示更好

- 推荐方案：`Three.js + glTF + WebSocket`。
- 选择理由：
  - 与当前 Vue 前端集成成本低，能快速做出可交互演示；
  - 适合毕业设计节奏，论文答辩时可展示“模型状态 + 3D轨迹 + 操作面板”的联动；
  - 后续可以平滑接入真实后端推理流（位置、速度、告警）。
- 已在项目中新增“3D仿真预览（演示）”页面作为骨架，后续可把占位渲染替换为真实 Three.js 引擎层。

## 14. 能否单独控制“缓慢环绕视角”的速度

- 已支持。
- 在 3D 仿真页面中，当镜头模式为“环绕观察”时，会显示独立滑杆：`环绕速度 (rad/s)`。
- 该速度与“仿真速度”解耦：
  - 仿真速度控制无人机运动快慢；
  - 环绕速度只控制相机绕场景旋转快慢。
- 页面新增“环绕周期(秒)”指标，方便快速调到答辩演示所需的慢速环绕效果。

## 15. 为什么环绕观察看起来没有绕着地图转

- 原因是相机朝向的 yaw 符号换算有误，导致“位置在绕圈，但画面不呈现正确环绕”。
- 已修复为：按当前投影坐标约定修正 yaw 计算，环绕镜头会持续朝向地图中心并绕地图旋转。
- 同时把默认环绕速度从 `0.08` 调整到 `0.12 rad/s`，更容易直观看到环绕效果。

## 16. 能否把现在的 2D 地图和模型推理先跑在 3D 展示里

- 已实现。
- 3D 页面已接入后端推理接口 `/api/run-demo`，可直接调用当前模型并加载回放数据。
- 渲染逻辑现在使用后端返回的：
  - `environment.obstacles` -> 3D 障碍方块
  - `frames[].agents` -> 3D 无人机位置与目标圈
- 也保留“加载2D示例”入口，便于在后端不可用时继续演示 3D 回放流程。

## 17. 如何把研究模式放到管理员里，并让系统界面更像真实产品

- 已实现“角色入口 + 权限视图”：
  - 右上角新增“执行者 / 管理员”切换按钮；
  - 研究模式仅管理员可见，执行者界面不显示研究入口。
- 页面风格已改为更简洁的控制台样式：
  - 顶部改为紧凑 `Top Bar`；
  - 精简了各页面说明性长文字；
  - 标签和面板尺寸收敛，减少“演示文档感”。

## 18. 3D 界面能否支持放大调节

- 已支持。
- 3D 页面新增“视图放大”滑杆（`x0.60 ~ x2.50`），可实时放大/缩小画面。
- 放大逻辑接入投影焦距参数，回放暂停时调节也会立即重绘生效。

## 19. 鼠标是否可以拖拽 3D 视角

- 已支持。
- 在 3D 画布中按住鼠标左键拖拽即可调整视角：
  - 环绕模式：水平拖拽改变环绕角度，垂直拖拽改变相机高度；
  - 跟随/俯视模式：拖拽可做视角偏航和俯仰微调。
- 拖拽时会暂停自动环绕增量，松开后继续按当前角度播放。

## 20. 能否切换到某个无人机视角并显示它看到的 3D 地图

- 已支持。
- 3D 页面新增“无人机视角”镜头模式，可选择具体无人机编号。
- 启用后会切到该无人机第一视角，并展示其局部 `11 x 11` 范围的 3D 地图内容（障碍、网格、无人机与目标）。
- 同时保留鼠标拖拽微调能力，便于观察该无人机的局部环境。

## 21. 为什么 3D 回放窗口突然空了

- 根因是相机俯仰角限制设置过窄，导致某些模式下相机看向了错误方向，画面看起来像“空白”。
- 已修复：
  - 放宽非无人机模式的俯仰角范围，恢复可见视角；
  - 无人机模式也同步放宽上限，避免边界角度卡死；
  - 新增“无可绘制地图数据”提示，防止再次出现黑屏无反馈。

## 22. 无人机视角里方块能否改成实体不透明，是否还有其他 bug

- 已改为实体不透明：
  - 在“无人机视角”模式下，障碍块从线框改为实体立方体（带顶面和侧面填充）；
  - 其他镜头模式继续保持线框，兼顾全局观察清晰度。
- 额外检查并修复：
  - 调整了无人机视角拖拽时的俯仰限制，避免过冲导致画面异常。
- 快速检查结果：
  - 前端构建通过（`npm --prefix web_frontend run build`）；
  - 当前未发现新的编译期错误。

## 23. 能否把无人机视角改成游戏里的第三人称跟随镜头

- 已改为第三人称跟随。
- 当前无人机视角行为：
  - 相机位于无人机后上方，始终跟随无人机移动；
  - 朝向优先跟随无人机移动方向，静止时回退到目标方向；
  - 拖拽可做偏航/俯仰微调，但镜头仍保持跟随主体。
- 为提升稳定性：
  - 增加了跟随平滑插值，减少突变抖动；
  - 切换到无人机视角时会重置跟随状态，避免残留旧镜头参数。

## 24. 无人机模式里为什么播放结束后不能再次播放

- 原因是回放结束时游标停在末帧，点击“播放”后会立即再次触发结束条件。
- 已修复为：当检测到当前在末帧时，点击“播放”会先重置到第 0 帧，再开始播放。

## 23. `https://github.com/alibaba/page-agent` 这个项目可以安装给 Codex 使用吗

- 结论：通常不能“直接”作为 Codex skill 安装。
- 原因：
  - Codex 的 skill 安装流程要求目标目录符合 skill 结构，并包含 `SKILL.md`；
  - `alibaba/page-agent` 当前是 TypeScript monorepo（有 `AGENTS.md`），不是现成的 Codex skill 包。
- 可行替代：
  - 作为普通项目依赖使用：在 Web 项目里按其 README 走 `npm install page-agent`；
  - 若要给 Codex 用，可额外封装一个 skill（`SKILL.md` + 对应脚本）再安装到 `$CODEX_HOME/skills`。

## 25. `page-agent` 的 Codex skill 现在封装完成了吗

- 已完成封装并安装。
- 已创建技能目录：`codex_skills/page-agent-codex/`，包含：
  - `SKILL.md`
  - `agents/openai.yaml`
  - `references/page-agent-integration.md`
- 已安装到全局目录：`~/.codex/skills/page-agent-codex/`。
- 备注：
  - 由于当前环境网络受限，`quick_validate.py` 依赖的 `PyYAML` 无法在线安装，未能执行官方校验脚本；
  - 已完成结构性手动校验，技能文件齐全且可被 Codex 发现，重启 Codex 后可通过 `$page-agent-codex` 调用。

## 26. 这个 skill 现在能不能直接跑通测试

- 结构测试：通过。
  - 已确认安装目录存在并包含 3 个关键文件：
    - `~/.codex/skills/page-agent-codex/SKILL.md`
    - `~/.codex/skills/page-agent-codex/agents/openai.yaml`

## 27. 把模型感知范围改成 `11 x 11 x 4` 里的 “4” 代表什么

- 这里的 `4` 不是“多看 4 层地图高度”，而是“同一个 `11 x 11` 局部窗口里有 4 个特征通道”。
- 可以把它理解成一张叠起来的四层小地图；每一层都还是 `11 x 11`，但每层表达的内容不同。
- 例如可设计为：
  - 第 1 层：障碍物
  - 第 2 层：其他智能体位置
  - 第 3 层：目标点位置
  - 第 4 层：规划路径或子目标引导
- 这样模型看到的不是单张二维图，而是“4 张对齐的 `11 x 11` 图一起输入网络”。
- 当前项目里训练主输入实际上更接近 `2 x 11 x 11`：
  - 一层是 `obstacles`
  - 一层是 `agents`
- 目标/规划引导目前没有作为独立通道分开，而是被写进现有观测里。
- 所以如果后续要改成 `11 x 11 x 4`，本质上就是把现在的输入扩展成 4 个通道，并明确每个通道各自承载什么语义。

## 28. 想让无人机可以上下移动，而不是只在平面前后左右移动，需要改什么

- 当前项目的核心训练环境是二维栅格，不是三维空间，所以“加上下移动”不是只多加两个动作键，而是要把环境、规划、观测、训练一起从 2D 改成 3D。
- 当前二维假设体现在这些地方：
  - 动作集合只有 `stay / up / down / left / right` 五个二维动作；
  - 位置坐标只有 `(x, y)`，没有 `z`；
  - 局部观测是 `11 x 11` 二维窗口，不是 `11 x 11 x depth` 三维体；
  - 规划器和冲突代价也都是在二维网格上算的。
- 如果要做“真实可升降的无人机”，至少要改下面几层：
  - 环境层：把位置、目标、障碍从 `(x, y)` 改成 `(x, y, z)`；
  - 动作层：把动作从 5 个扩成至少 7 个，例如 `停留 / 前 / 后 / 左 / 右 / 上升 / 下降`；
  - 观测层：把当前二维局部窗口改成三维局部体，或改成“多层高度切片”；
  - 规划层：把 A* / LB-A* / 重规划逻辑改成 3D 邻接搜索；
  - 训练层：重新训练模型，因为旧模型只学过二维动作与二维状态；
  - 前端层：3D 页面目前主要是“把 2D 回放渲染成 3D 视觉”，不是真 3D 环境，所以也要改数据结构与回放逻辑。
- 结论：
  - 如果你要的只是“3D 页面里视觉上能升降”，可以只改前端回放；
  - 如果你要的是“训练出来的模型真的会升高/降低飞行”，那就是一次 2D -> 3D 环境升级，需要改训练主链路，不能只改前端。

## 29. SMAPO 这类训练框架主要是为了解决什么场景的问题，为什么只有 2D

- 从论文思路和当前项目文档来看，SMAPO 主要针对的是 **MAPF / LMAPF** 场景：
  - 多个智能体在共享环境中长期执行路径任务；
  - 目标会持续变化或被重复分配；
  - 需要同时兼顾无冲突、吞吐量和实时性。
- 更贴近的典型场景其实是：
  - 仓储机器人调度
  - 园区/厂区运输车协同
  - 路网中的自动车辆长期导航
  - 其他“很多个体在同一张离散地图上反复接任务”的场景
- 你的课题把它迁移理解成“无人机动态调度”是可以的，但当前训练实现本质上仍然沿用了论文里的 **二维离散路径规划抽象**。
- 为什么是 2D，核心原因有 4 个：
  - **问题建模本身就是图搜索 / 网格搜索**：论文把环境抽象成图 `G=(V,E)` 或二维栅格，冲突通常定义为顶点冲突和边冲突；
  - **训练与规划成本可控**：2D 状态空间小很多，A* / LB-A* / MARL 训练都更容易收敛；
  - **基准数据与主流对比大多是 2D**：迷宫图、随机障碍图、MovingAI 等常见 MAPF 基准都是二维地图；
  - **论文关注的是“协同避碰与长期调度”**，不是飞行动力学本身：重点在多智能体调度和冲突消解，而不是空气动力学或三维飞控。
- 所以这里的“2D”并不代表场景只能是地面车，也不代表不能类比到无人机，而是说：
  - 论文先把复杂真实世界抽象成二维离散导航问题；
  - 先解决“多机长期协同走路由”的核心决策难题。
- 当前仓库为什么也是 2D：
  - 环境动作定义只有二维 5 动作；
  - 位置是 `(x, y)`，不是 `(x, y, z)`；
  - 局部观测是 `11 x 11`；
  - C++ 规划器和重规划器也都是二维邻接搜索。
- 如果以后要做真实无人机三维飞行，可以把 SMAPO 看成一个“二维协同决策框架原型”：
  - 它解决的是多机协同规划思想；
  - 但真正升级到 3D，还需要重新设计状态、动作、观测和规划器。

## 30. 将当前 2D 训练框架迁移为 3D 的复杂度和可行性如何

- 结论先说：
  - **可行**，但只在“研究型重构项目”意义上可行；
  - **复杂度高**，不是局部改造；
  - 如果目标是“短期做出能训练的 3D 版本”，当前仓库的直接迁移性只能算 **中低**；
  - 如果目标是“先做近似 3D / 分层高度版本”，可行性会明显更高。
- 复杂度高的原因在于，当前仓库几乎整条主链路都带有二维假设：
  - 环境层：位置、目标、障碍、局部观测都是 2D；
  - 动作层：默认只有 5 个二维离散动作；
  - 规划层：Python 重规划和 C++ LB-A* 都是二维邻接搜索；
  - 模型层：邻居相对位置是 `(64, 2)`，并使用 `RelativeEmbedding2D`；
  - 训练增强层：DT 只支持二维旋转/镜像，并且默认要求动作数为 5。
- 从工程角度看，迁移成本大致可以分成 3 档：
  - **低到中等复杂度：2.5D / 分层高度近似**
    - 思路：仍以二维平面为主，只额外加入少量离散高度层；
    - 例如把地图做成 `11 x 11 x 4` 高度切片，动作增加“升一层 / 降一层”；
    - 规划器可以先改成“多层网格图搜索”，难度低于连续 3D；
    - 这是最适合当前仓库演进的方向。
  - **高复杂度：真 3D 离散体素环境**
    - 思路：把环境彻底改成 `(x, y, z)` 体素空间；
    - 观测改成局部 3D 体，规划器改成 3D A*，邻居关系改成 3D 相对位置；
    - 模型和数据增强都要重写一部分；
    - 这是“能做，但改动面很大”的方案。
  - **极高复杂度：接近真实无人机动力学的 3D**
    - 不只是路径格子，还要加入速度、姿态、爬升率、碰撞半径、动力学约束；
    - 这已经不再是当前 SMAPO 框架的小改版，而是新课题规模。
- 如果按当前代码结构来评估：
  - **环境与观测改造复杂度：高**
    - 因为 `Grid/Pogema/SMAPO` 都默认 `(x, y)` 和二维窗口；
  - **规划器改造复杂度：很高**
    - 因为底层 `planner/LB_A` 是 C++ 二维实现，重写成本高；
  - **模型改造复杂度：中高**
    - 卷积输入还能改，但 `relative_xy`、注意力偏置、动作头都要跟着改；
  - **训练策略改造复杂度：中高**
    - 当前 DT 增强强绑定二维旋转与 5 动作，需要重新定义 3D 对称变换；
  - **重新训练成本：高**
    - 旧模型权重基本不能直接复用，必须重新训练。
- 综合判断：
  - **做“论文答辩级可运行原型”**：建议走 **2.5D / 分层高度**，可行性高于真 3D；
  - **做“严格意义上的 3D SMAPO”**：可行，但属于一次大重构；
  - **做“真实无人机飞控级 3D”**：当前仓库不适合作为直接迁移底座。
- 如果用一句话概括：
  - 当前 2D -> 3D 迁移不是“升级参数”，而是“重写环境核心 + 规划核心，再适配模型和训练”。

## 31. 从当前 2D 框架迁移到 3D，可以考虑哪 3 种方案

- 可以把方案分成 3 档：
  - `2.5D / 分层高度`
  - `真 3D 离散体素`
  - `真实飞控级 3D`

### 31.1 方案一：2.5D / 分层高度

- 这是最适合当前仓库渐进升级的方案。
- 核心思想：
  - 仍保留二维主平面 `(x, y)`；
  - 额外增加少量离散高度层 `z`，例如 3 层、4 层或 5 层；
  - 智能体不再只在单层平面移动，而是可以“升一层 / 降一层”。
- 典型建模方式：
  - 状态：`(x, y, z)`；
  - 动作：`停留 / 前 / 后 / 左 / 右 / 上升 / 下降`；
  - 观测：可以做成：
    - 多层切片：例如 `11 x 11 x 4`；
    - 或“当前层 + 上一层 + 下一层 + 路径层”这类多通道输入。
- 对当前代码的改动重点：
  - 环境位置与目标从 2D 扩到 3D；
  - `MOVES` 从 5 动作扩到 7 动作；
  - `SMAPO` 预处理里的相对坐标和 BFS 改成三维版本；
  - 规划器可先做“多层图搜索”，不一定立刻上连续 3D A*；
  - 模型输入通道数增加，动作头输出维度改为 7；
  - 训练增强 DT 暂时可以关掉，或者只保留一部分安全变换。
- 优点：
  - 对当前 2D 框架改动最小；
  - 容易保留原有网格抽象和很多训练代码；
  - 更适合作为毕业设计和阶段性原型；
  - 计算量远小于真 3D。
- 缺点：
  - 仍然是近似 3D，不是真实连续空间飞行；
  - 高度层太少时，表达能力有限；
  - 需要重新设计“不同高度层之间是否互相冲突”的规则。
- 适合的目标：
  - 论文原型；
  - 答辩演示；
  - 先验证“多无人机可升降协同决策”是否有效。

### 31.2 方案二：真 3D 离散体素

- 这是严格意义上的 3D 网格化路径规划方案。
- 核心思想：
  - 整个环境不再是二维地图，而是三维体素空间；
  - 障碍、智能体、目标都存在于 `(x, y, z)` 网格中；
  - 智能体在 3D 空间里做离散移动。
- 典型建模方式：
  - 状态：三维体素坐标；
  - 动作：可定义为 7 动作或更多，例如：
    - `停留`
    - `x+/x-`
    - `y+/y-`
    - `z+/z-`
  - 观测：
    - 局部体素块，例如 `11 x 11 x 5`；
    - 或 3D 多通道体输入。
- 对当前代码的改动重点：
  - `Pogema/Grid` 要从二维数组改成三维数组；
  - 局部观测裁切逻辑要支持三维；
  - `SMAPO` 的 `relative_xy` 要升级成 `relative_xyz`；
  - 模型里的 `RelativeEmbedding2D` 要改成 3D 版本；
  - Python 重规划和 C++ `LB_A` 都要改成 3D 邻接搜索；
  - DT 数据增强要重新定义，因为二维旋转/镜像不再直接适用。
- 优点：
  - 语义最完整，真正支持三维离散路径规划；
  - 更接近“无人机在空间中穿行”的任务本质；
  - 后续往更真实无人机场景迁移更自然。
- 缺点：
  - 环境和规划器重写量很大；
  - 训练成本显著上升；
  - 原有二维经验和增强策略复用度下降；
  - 调参难度会明显增加。
- 适合的目标：
  - 研究导向项目；
  - 需要强调“严格 3D 规划”而不是“类比无人机调度”的论文。

### 31.3 方案三：真实飞控级 3D

- 这是最贴近真实无人机的方案，也是最重的一档。
- 核心思想：
  - 不再把无人机简化成“格子里的点”；
  - 需要考虑速度、加速度、姿态、爬升率、转弯半径、安全间隔等动力学约束。
- 典型建模方式：
  - 状态不只是 `(x, y, z)`，还可能包括：
    - 速度
    - 航向角
    - 爬升率
    - 剩余能量
  - 动作也不再只是简单离散方向，可能变成：
    - 离散控制指令；
    - 或连续控制量。
- 对当前代码的改动重点：
  - 现有 `MAPF/LMAPF` 网格框架基本只能保留很少一部分思路；
  - 环境、奖励、规划器、模型输出、碰撞检测都要重做；
  - 训练方法也可能要从当前离散 PPO 改到连续控制或混合控制。
- 优点：
  - 与真实无人机系统最贴近；
  - 工程解释力最强；
  - 后续接数字孪生、飞行仿真平台更合理。
- 缺点：
  - 已经不属于“在当前仓库上升级”；
  - 更接近重新做一套新系统；
  - 数据、算力、调试成本都非常高。
- 适合的目标：
  - 长周期研究项目；
  - 真正面向工程落地或高仿真验证。

### 31.4 三种方案怎么选

- 如果你的目标是：
  - **尽快做出可训练、可解释、可答辩的版本**：
    - 选 `2.5D / 分层高度`
- 如果你的目标是：
  - **论文里明确强调“3D 离散路径规划”**：
    - 选 `真 3D 离散体素`
- 如果你的目标是：
  - **最终接近真实无人机飞行控制系统**：
    - 选 `真实飞控级 3D`

### 31.5 对当前项目的建议

- 结合你现在这套仓库，我最推荐的是：
  - 第一阶段先做 `2.5D / 分层高度`
  - 第二阶段如果效果稳定，再继续升级到 `真 3D 离散体素`
- 不建议直接从当前版本一步跳到“真实飞控级 3D”，因为那样很容易把原本已经能跑通的训练链路全部打散。

## 32. 方案一里，无人机能否在 `100 x 100 x 4` 这样的 3D 空间中飞行

- 可以，但要先明确：这里的 `100 x 100 x 4` 指的是 **离散分层空间**，不是连续真实 3D 空域。
- 也就是说，在方案一里：
  - `100 x 100` 是平面范围；
  - `4` 是 4 个离散高度层；
  - 无人机是在这 4 层之间做“升一层 / 降一层”和平面移动。
- 因此：
  - **可以支持在 `100 x 100 x 4` 这类分层 3D 地图中飞行**；
  - 也可以表达跨层避障、上下绕行、不同高度层分流等行为。
- 但它不能等同于“真实连续 3D 飞行”，因为：
  - 高度只有 4 个离散层，不是任意连续高度；
  - 动作通常还是离散的格点移动，不包含真实速度、姿态和转弯动力学；
  - 更像“多层空中走廊导航”，而不是物理飞控。
- 从规模上看，`100 x 100 x 4 = 40000` 个离散位置：
  - 对方案一来说，这个规模在建模上是合理的；
  - 但训练和规划负担会明显高于二维 `100 x 100`；
  - 尤其当无人机数量很多时，冲突消解和重规划成本会上升。
- 所以结论可以概括为：
  - **能飞**：如果你接受“4 层离散高度”的近似 3D；
  - **不能完全等同真实 3D 飞行**：因为它仍然是分层网格导航模型。
    - `~/.codex/skills/page-agent-codex/references/page-agent-integration.md`
  - 已用 Ruby 成功解析 `openai.yaml` 与 `SKILL.md` frontmatter。
- 端到端调用测试：当前环境未跑通。
  - 复测命令：`codex exec --sandbox read-only --output-last-message /tmp/page_agent_skill_test.txt 'Use $page-agent-codex and reply with exactly SKILL_OK'`
  - 失败原因：当前终端环境访问 Codex 后端连接中断（`stream disconnected`），并伴随 `.codex` 状态写入权限告警，导致无法完成模型响应链路。
- 结论：
  - skill 文件本身可用；
  - 在“当前受限环境”下无法完成在线端到端响应测试，需要在可访问 Codex 后端且本地权限正常的终端/桌面会话中复测。

## 27. 管理员和执行者如何做“真实切换界面”

- 已实现为“弹窗确认式切换”，不是简单按钮瞬时切换。
- 当前交互流程：
  - 右上角显示当前身份（执行者/管理员）；
  - 点击“切换身份”打开弹窗；
  - 在弹窗里选择角色并点击“确认切换”后生效。
- 权限行为：
  - 执行者：仅可见“运营中心”和“3D回放”；
  - 管理员：在执行者能力基础上，额外可见“研究模式”。

## 28. 作为毕业设计系统，下一步该补哪些内容（优先级清单）

- 建议按 `P0 -> P1 -> P2` 推进，先保证“可演示、可闭环、可量化”。

- `P0（答辩前必须补齐）`
  - 任务闭环后端：从“创建任务 -> 下发 -> 执行 -> 完成/失败”全链路可跑通；
  - 实时状态通道：前端不再只看静态示例，接入实时运行状态推送；
  - 数据落库：任务记录、轨迹、关键指标、日志可持久化并可查询；
  - 异常与安全基础策略：失联/冲突/超时至少有告警与降级处理；
  - 评估看板：吞吐量、累计任务数、冲突数、平均耗时等核心指标可视化。

- `P1（系统化增强）`
  - 登录与角色权限：管理员/执行者权限真正后端生效并可审计；
  - 设备管理：无人机注册、在线状态、心跳、固件/模型版本管理；
  - 模型版本管理：模型切换、回滚、参数模板、实验对比；
  - 地图管理：多地图版本、禁飞区/障碍配置、发布流程。

- `P2（工程化与上线能力）`
  - 自动化测试体系：单元、集成、E2E 与回归基线；
  - 可观测性：统一日志、链路追踪、告警阈值与通知；
  - CI/CD 与运维：自动构建发布、配置分环境、备份恢复。

## 30. “先完成 P0”目前落地了哪些

- 已落地，范围覆盖你要求的 5 项：
  - 任务闭环后端：支持创建任务、开始、暂停、继续、停止；
  - 实时状态通道：新增 SSE 事件流，前端可实时接收运行帧和状态；
  - 数据落库：任务、事件、告警写入 SQLite；
  - 异常与安全基础：冲突、卡滞、低吞吐超时三类基础告警；
  - 评估看板：运营页面实时显示在线数、累计任务、冲突告警、平均时延，并展示告警列表。

- 新增后端接口（运营模式）：
  - `POST /api/tasks`
  - `GET /api/tasks`
  - `GET /api/tasks/{id}`
  - `POST /api/tasks/{id}/control`
  - `GET /api/tasks/{id}/events`（SSE）
  - `GET /api/tasks/{id}/alerts`
  - `GET /api/dashboard/summary`

- 说明：
  - 运营模式已从“纯前端本地回放”切到“后端任务驱动”；
  - `source=model` 模式若推理失败会自动降级到 `sample`，并写入 warning，保证演示链路不中断。

## 31. 这个训练需不需要“训练数据”

- **不需要传统意义上的标注训练数据集**，因为当前 SMAPO 主训练链路是强化学习，不是监督学习。
- 从代码上看：
  - 训练算法是 `APPO`，配置在 `learning/learning_config.py` 中，`algo: APPO`；
  - 训练时环境是动态创建的，入口在 `learning/ppo.py` 和 `env/create_env.py`；
  - 地图、障碍物、起点和目标主要由环境在线生成，相关逻辑在 `pogema/generator.py`。
- 这意味着模型不是“读取一堆图片或表格样本来拟合标签”，而是：
  - 在环境里不断和地图交互；
  - 执行动作；
  - 拿到奖励；
  - 再根据轨迹更新策略。
- 所以它不需要准备类似“训练集 / 验证集 / 测试集”的人工标注数据文件，但它仍然需要**训练场景来源**，包括：
  - 随机生成地图；
  - 或固定地图集合；
  - 或你自己定义的任务场景。
- 如果说“训练输入”是什么，那更准确地讲是：
  - 每一步环境给出的局部观测；
  - 当前任务目标；
  - 多智能体交互产生的在线轨迹。
- 一句话总结：
  - **不需要监督学习那种离线训练数据集**；
  - **需要环境场景和交互过程**，训练数据是在运行中“自己采出来”的。

## 32. 当前训练场景是否已经改成 2.5D

- **结论：已经具备 2.5D 训练模式，但当前默认训练场景仍然是 2D。**
- 原因是默认配置里 `height_levels = 1`，这意味着如果不显式修改配置，训练时还是单层地图。
- 从代码接入情况看：
  - `learning/learning_config.py` 已加入 `height_levels` 配置项；
  - `pogema/envs.py` 会在 `height_levels > 1` 时切换到分层观测和 7 动作；
  - `env/SMAPO.py` 和 `env/planning.py` 已支持分层状态与分层规划。
- 但当前场景生成方式还不是“完全原生 3D 地图”，而是：
  - 地图障碍物仍先按二维生成；
  - 再在多层模式下复制到各个高度层使用；
  - 智能体起点/终点会被提升为 `(x, y, z)` 的分层坐标。
- 这意味着现在的 2.5D 场景更准确地说是：
  - **多层二维地图**
  - 而不是每一层障碍都不同的“独立 3D 体素地图”。
- 所以如果你问“当前训练场景有没有改为 2.5D”，答案要分两句说：
  - **训练框架层面：已经支持 2.5D；**
  - **默认训练配置层面：还没有默认切到 2.5D；**
  - **地图生成层面：目前是二维障碍复制到多层，不是每层独立建图。**

## 33. `100 x 100 x 4` 是不是地图大小

- **是，但更准确地说，是“分层地图空间大小”。**
- 其中：
  - `100 x 100` 表示平面地图大小；
  - `4` 表示高度层数。
- 所以 `100 x 100 x 4` 的意思不是单张二维地图，而是：
  - 有 `4` 层；
  - 每一层都是 `100 x 100` 的地图；
  - 总共构成一个分层空间。
- 它和局部观测窗口 `11 x 11` 不是一回事：
  - `100 x 100 x 4` 说的是**整个环境有多大**；
  - `11 x 11` 或 `11 x 11 x 4` 说的是**单个无人机一次能看到多大范围**。
- 例如：
  - 地图可以是 `100 x 100 x 4`；
  - 但每个无人机每一步只看自己周围的 `11 x 11 x 4` 局部观测。

## 34. 能不能把地图生成改成原生 3D，而不是每一层复制

- **可以实现，但复杂度比“分层复制”明显更高。**
- 当前代码还不算原生 3D，主要原因是：
  - `pogema/generator.py` 里的 `generate_obstacles` 仍然只生成二维障碍矩阵；
  - `generate_positions_and_targets_fast` 里的连通域搜索和起终点生成也还是二维；
  - `env/planning.py` 里的 `LayeredPlanner` 虽然支持 `(x, y, z)` 位置，但障碍判断仍然是按二维 `self.obstacles[x][y]`；
  - `env/SMAPO.py` 里的 `bfs_obs_3d` 也还是按二维障碍 `obstacle[nx_x, nx_y]` 判断。
- 所以如果要改成“原生 3D 地图”，需要一起改这几层：
  - 地图生成：障碍物从二维数组改成三维体素数组；
  - 起点/终点生成：从二维连通域改成三维连通域；
  - 环境存储：`Grid` 里的全局障碍需要支持每层不同；
  - 局部观测：不能再把二维障碍复制到各层，而要裁剪真实 3D 局部块；
  - 规划器：碰撞检测和可达性判断都要改成按 `(x, y, z)` 查询障碍；
  - 预处理：BFS 邻域搜索、路径注入等逻辑要按真实 3D 障碍运行。
- 好消息是：
  - 你现在的 2.5D 改造已经把动作、位置、局部观测张量、相对位置编码接到了三维坐标语义；
  - 所以“模型输入链路”不需要从零重做；
  - 难点主要集中在**地图生成和规划逻辑**，不是神经网络本身。
- 因此更准确的判断是：
  - **技术上可实现；**
  - **工程上属于中高复杂度升级；**
  - **不是只改一个生成函数就够，而是要把障碍语义从二维彻底提升到三维。**

## 35. GitHub 上有没有可以直接用的 3D 地图生成项目

- **有，但要区分“3D 占据地图库”和“3D 场景生成器”。**
- 如果你的目标是给当前 SMAPO 改成“原生 3D 障碍地图”，最相关的是前者；如果你想生成复杂 3D 城市、室内场景，则更偏后者。

- 适合当前项目方向的仓库：
  - **PyOctoMap**
    - GitHub: `https://github.com/Spinkoo/pyoctomap`
    - 特点：Python 封装的 OctoMap，支持 3D 占据建图、概率更新、碰撞检测和射线查询；
    - 安装相对直接：README 提供 `pip install pyoctomap`；
    - 结论：**和你当前 Python 训练代码最容易衔接**，适合作为“原生 3D 障碍存储层”。
  - **OctoMap**
    - GitHub: `https://github.com/OctoMap/octomap`
    - 特点：经典 3D 八叉树占据地图框架，成熟度高；
    - 结论：适合做底层标准方案，但你当前项目若直接接入，需要 Python/C++ 适配。
  - **UFOMap**
    - GitHub: `https://github.com/UnknownFreeOccupied/ufomap`
    - 特点：显式表示 unknown/free/occupied 三种空间，适合路径规划和避障；
    - 结论：如果你后面想把“未知空间”也纳入训练语义，它比简单体素数组更有优势。
  - **wavemap**
    - GitHub: `https://github.com/ethz-asl/wavemap`
    - 特点：多分辨率 3D 占据建图，官方说明现在比论文时期快很多、内存占用更低；
    - 结论：更偏工程级 3D 地图底座，性能强，但接入复杂度高于 PyOctoMap。
  - **map_manager**
    - GitHub: `https://github.com/Zhefan-Xu/map_manager`
    - 特点：ROS/C++ 的 3D occupancy voxel map / ESDF / dynamic map 库，还带轻量模拟器；
    - 结论：适合作为“无人机 3D 建图系统”的参考实现，但不适合直接塞进你当前纯 Python 训练链路。

- 更适合“场景生成/体素化”的仓库：
  - **VoxCity**
    - GitHub: `https://github.com/kunifujiwara/VoxCity`
    - 特点：Python 包，可自动下载地理数据并体素化城市、树木、地形；
    - 结论：如果你想做真实城市空域训练场景，它很有价值。
  - **voxelmap**
    - GitHub: `https://github.com/moxilang/voxelmap`
    - 特点：Python 库，可从 NumPy 数组构建 voxel 模型，安装简单；
    - 结论：更适合做体素表示和可视化，不是完整地图生成算法，但很适合你自己生成 3D 障碍后做导出与调试。
  - **Mesh Vox**
    - GitHub: `https://github.com/Septaris/mesh_vox`
    - 特点：把 STL 模型体素化成 voxel 数组；
    - 结论：如果你有现成 3D 模型，想快速转成体素障碍图，这个很实用。
  - **BlenderProc**
    - GitHub: `https://github.com/DLR-RM/BlenderProc`
    - 特点：程序化生成 3D 场景并渲染训练数据；
    - 结论：更适合视觉数据合成，不是占据地图库，但可用来生成复杂场景再转体素。
  - **scene_synthesizer**
    - GitHub: `https://github.com/NVlabs/scene_synthesizer`
    - 特点：Python 场景生成包；
    - 结论：更偏操作/室内场景合成，可作为 3D 几何来源。

- 额外参考仓库：
  - **monocular-slam-drone**
    - GitHub: `https://github.com/jonasctrl/monocular-slam-drone`
    - 特点：从单目深度估计构建 3D voxel occupancy grid，并在 AirSim 中做无人机规划；
    - 结论：不是通用地图生成库，但非常接近“无人机 + 3D 占据图 + 路径规划”的应用链路。

- 综合建议：
  - 如果你要**最快接到当前 SMAPO Python 框架**：优先看 `PyOctoMap`；
  - 如果你要**最成熟的经典 3D 占据地图底座**：看 `OctoMap` / `UFOMap`；
  - 如果你要**真实城市体素场景**：看 `VoxCity`；
  - 如果你要**从现成 3D 模型快速转 voxel**：看 `Mesh Vox` 或 `voxelmap`。

- 最重要的一句：
  - **我没有查到一个能和你当前 SMAPO 训练代码“零改造直接替换”的现成仓库；**
  - **最现实的做法是借用其中一个 3D 地图/体素库，然后把输出适配成你当前训练环境使用的 3D 障碍张量。**

## 36. PyOctoMap 是离散地图还是非离散地图

- **结论：PyOctoMap 本质上是离散地图。**
- 但它不是“普通的稠密三维数组地图”，而是：
  - **基于八叉树（octree）的离散占据地图**；
  - 每个节点对应一个体素空间块；
  - 地图支持多分辨率。
- 之所以容易误解成“非离散”，是因为它的接口用的是连续坐标，例如：
  - `tree.updateNode([1.0, 2.0, 3.0], True)`
  - `tree = pyoctomap.OcTree(0.1)`
- 但这里的连续坐标最终会按照设定分辨率（例如 `0.1m`）映射到体素单元中，所以底层仍然是离散占据表示。
- 更准确地说，PyOctoMap 兼具这两个特点：
  - **输入坐标是连续实数坐标；**
  - **内部地图表示是离散体素 / 八叉树单元。**
- 和你当前项目的关系：
  - 它比 `numpy[z, x, y]` 这种固定大小稠密网格更节省内存；
  - 但如果你要接到当前 SMAPO 训练环境，通常还是需要把 OctoMap 里的占据结果再导出或采样成训练代码能直接读取的离散 3D 张量。

## 37. PyOctoMap 是否已经接入当前地图生成系统

- **已经接入，当前实现方式是“PyOctoMap 生成原生 3D 占据图，再导出为训练环境使用的稠密 3D 障碍张量”。**
- 当前新增了以下配置语义：
  - `height_levels > 1`
  - `native_3d_obstacles = True`
  - `obstacle_backend = pyoctomap`
  - `octomap_resolution`
- 接入后的行为是：
  - 地图生成器不再按二维障碍复制到每层；
  - 而是直接生成 `height_levels x size x size` 的原生 3D 障碍体素图；
  - 起点/终点采样改为在 3D 连通空间中采样；
  - `Grid / Planner / SMAPO` 都按真实 `(x, y, z)` 障碍查询。
- 但当前实现仍然保留了训练兼容层：
  - 训练环境内部最终仍然读取稠密体素张量；
  - 没有把 OctoMap 对象直接贯穿到神经网络和规划器内部。
- 所以更准确地说：
  - **PyOctoMap 已被接入为“原生 3D 地图生成后端”；**
  - **训练侧仍使用导出的离散 3D 障碍张量继续运行。**

## 38. PyOctoMap 接入后，Docker 中测试结果如何

- 已完成两类 Docker 测试。

- `1. 环境 smoke 测试`
  - 命令：`docker run --rm smapo:pyoctomap-test python scripts/smoke_pyoctomap_env.py`
  - 结果通过；
  - 关键输出：
    - `pyoctomap_smoke_ok`
    - `global_obstacles_shape=(4, 16, 16)`
    - `single_obs_shape=(8, 11, 11)`
    - `agent_position_example=(12, 13, 3)`
- 这说明：
  - 原生 3D 障碍图已经成功生成；
  - 局部观测成功进入训练前处理链路；
  - 智能体位置已是三维坐标。

- `2. 训练入口 smoke 测试`
  - 在 Docker 中执行了最小化 `main.py` 训练启动；
  - 结果成功走到：
    - learner 初始化；
    - policy worker 初始化；
    - actor/env runner 初始化；
    - 环境 reset 和采样启动；
    - 短时训练流程退出。
- 训练日志里出现了 `Model saving request timed out`，但最终进程正常退出并打印 `Done!`：
  - 这更像是超短训练时长下的保存超时提示；
  - 不是 PyOctoMap 接入导致的地图或环境错误。

- 这次 Docker 测试还验证出一个部署细节：
  - 当前 `aarch64` Docker 环境下，`pyoctomap` 没有现成 wheel；
  - 需要额外安装系统库：
    - `liboctomap-dev`
    - `libdynamicedt3d-dev`
- 补上这两个依赖后，`pyoctomap` 已可在 Docker 内成功编译安装。

## 39. 当前训练完整路径是否已经全部适配 2.5D / 原生 3D

- **结论：还没有全部适配完成。**
- 现在的状态更准确地说是：
  - **`use_maps=False` 的训练主链可以启动；**
  - **但默认训练路径和规划链里仍有关键 2D 假设残留。**

- 我这次做了完整链路审计后，确认至少有两处高风险问题：

- `1. 默认训练路径（EnvironmentMazes / use_maps=True）还没有适配完成`
  - 默认实验配置仍然是 `EnvironmentMazes()`，见 `learning/learning_config.py`；
  - 该配置默认 `use_maps=True`，并从 `MAPS_REGISTRY` 读取二维地图；
  - 但在 2.5D / 3D 模式下，`pogema/generator.py` 会把动作增广为三维 `dx,dy,dz`；
  - 当地图仍是二维时，`bfs()` 里的二维循环 `for dx, dy in moves` 会直接报错。
- 我在 Docker 中实际验证时，`use_maps=True + height_levels=4 + native_3d_obstacles=True` 会在 reset 阶段报：
  - `ValueError: too many values to unpack (expected 2)`
- 这说明：
  - **当前默认训练路径并没有打通；**
  - 只有我们手动切到 `use_maps=False` 的路线，训练才不会在这个点直接崩掉。

- `2. 即使 use_maps=False，SMAPO 的规划器仍没有真正吃到正确的 3D 障碍张量`
  - `env/create_env.py` 里的 `ProvideGlobalObstacles.get_global_obstacles()` 会把障碍转成 `.tolist()`；
  - `env/SMAPO.py` 的 `reset_state()` 再把这个结果传给 `LayeredPlanner`；
  - 但 `env/planning.py` 里的 `LayeredPlanner` 只有在障碍对象带 `.shape` 且是三维时，才会走真实 3D 分支。
- 也就是说：
  - 真实 3D 障碍先被转成了 Python 嵌套列表；
  - 规划器随后把它当成二维结构去解析。
- 我在 Docker 中直接检查 planner 内部状态时，拿到的结果是：
  - 障碍类型是 `list`
  - `has_shape=False`
  - 首个智能体位置示例是 `(6, 11, 2)`
  - 对应路径长度却只有 `1`
  - 路径内容只有当前位置 `[(6, 11, 2)]`
- 这说明：
  - **训练虽然能启动，但 SMAPO 的重规划辅助在 3D 模式下实际上已经退化了；**
  - 很多时候规划器并没有真的在正确的 3D 障碍空间里搜索。

- 还有一个中等风险边界：
  - 当前 `MultiMapWrapper` 和 `Grid.map` 读入的地图本身仍是二维；
  - 所以即便把默认 `use_maps=True` 那条崩溃链修掉，也还不能直接得到“原生 3D 地图集训练”；
  - 现有地图集需要额外设计成 3D 版本，或定义二维地图升维规则。

- 所以最终判断是：
  - **不是全部完成；**
  - **环境初始化和最小训练启动已可运行；**
  - **但默认训练路径和 3D 规划辅助链路还没有完全闭环。**

- 如果按优先级来修，应该先做这两件事：
  - 先修 `use_maps=True` 的默认训练路径，让二维地图集在 2.5D 模式下至少不崩；
  - 再修 `ProvideGlobalObstacles -> SMAPO -> LayeredPlanner` 这一段，确保 planner 真正收到 NumPy 3D 障碍张量，而不是 Python list。

## 40. 现在 2.5D / 原生 3D 训练主链路修好了吗，复测结果如何

- **结论：这次已经把上次审计里确认的两处主断点修掉了。**
- 当前更准确的状态是：
  - `use_maps=False + native_3d_obstacles=True + obstacle_backend=pyoctomap` 的原生 3D 随机场景训练链路已打通；
  - 默认 `EnvironmentMazes / use_maps=True` 在 `height_levels=4` 下也已能正常 reset、规划、启动训练；
  - 但现有地图集本身仍是二维地图集，所以 `use_maps=True` 这条链现在是 **2.5D 分层 fallback**，不是“原生 3D 地图集训练”。

- 这次修掉的两个关键问题是：

- `1. 默认 mazes 路径不再因为二维地图 + 三维动作而在 reset 时崩溃`
  - 之前 `pogema/generator.py` 的连通域搜索会直接拿 `get_action_deltas()`；
  - 当 `height_levels > 1` 时动作变成三维 `(dx, dy, dz)`，但二维 `bfs()` 仍按 `dx, dy` 解包，所以 `use_maps=True` 会直接报错。
  - 现在已经改成：
    - **二维障碍图使用二维连通域动作；**
    - **三维障碍图使用三维连通域动作。**
  - 这样默认 mazes 地图即使在 2.5D 模式下，也能正常生成起点终点并继续训练。

- `2. SMAPO -> planner 的障碍传递链已经保留 NumPy / shape 信息`
  - 之前 `ProvideGlobalObstacles.get_global_obstacles()` 会把障碍转成 `.tolist()`；
  - `LayeredPlanner` 因此拿不到 `.shape`，很多时候退回二维逻辑。
  - 现在已经改成：
    - 上游直接传 `np.ndarray`；
    - planner 内部再统一标准化障碍张量。
  - 这样 `LayeredPlanner` 在原生 3D 随机场景下已经能真正收到 `shape=(levels, height, width)` 的三维障碍张量。

- 这次 Docker 复测结果如下：

- `1. 原生 3D 随机场景 smoke`
  - 执行：`docker run --rm smapo:pyoctomap-test python scripts/smoke_pyoctomap_env.py`
  - 结果通过；
  - 关键输出：
    - `pyoctomap_smoke_ok`
    - `global_obstacles_shape=(4, 16, 16)`
    - `single_obs_shape=(8, 11, 11)`

- `2. 双链路 smoke`
  - 执行：`docker run --rm smapo:pyoctomap-test python scripts/smoke_2p5d_training_paths.py`
  - 结果通过；
  - 关键输出：
    - `random_native_3d` 的 planner 障碍形状为 `(4, 26, 26)`，说明 planner 收到的是三维障碍；
    - `mazes_layered` 的 planner 障碍形状为 `(75, 75)`，说明二维地图集路径已稳定走 2.5D fallback；
    - 两条路径的 `path_stats.num_nontrivial` 都大于 0，说明规划输出不再退化成全员长度 1。

- `3. 短时训练 smoke`
  - 原生 3D 随机场景：
    - `python main.py --env=Pogema-v0 ... --target_num_agents=64 --use_maps=False --height_levels=4 --native_3d_obstacles=True --obstacle_backend=pyoctomap`
  - 默认 mazes 2.5D：
    - `python main.py --env=PogemaMazes-v0 ... --target_num_agents=128 --height_levels=4 --native_3d_obstacles=True --obstacle_backend=pyoctomap`
  - 两条命令都成功走到：
    - learner 初始化；
    - actor / policy worker 初始化；
    - env reset；
    - `Collecting experience...`；
    - 最终 `Done!` 退出。

- 所以现在可以这样理解：
  - **训练主链路已经闭环；**
  - **原生 3D 已适用于随机生成地图；**
  - **默认 mazes 已适用于 2.5D 分层训练；**
  - **但 `use_maps=True` 还不是“原生 3D 地图集”。**

## 41. 为什么我在 main.py 里改了 num_agents，训练启动时还是按 64 / 128 个 agent 在跑

- 这是当前训练入口里的一个旧配置机制，不是 2.5D 改造带来的新问题。
- 在 `utils/training_tools.py` 里，只要：
  - `agent_bins` 不为 `None`
  - 且 `target_num_agents` 不为 `None`
- 训练入口就会优先用 `agent_bins` 来覆盖 `environment_config.grid_config.num_agents`。

- 具体逻辑是：
  - 如果 `env_id is None`，直接取 `agent_bins[0]`；
  - 所以：
    - 普通 `Environment` 默认会被拉到 `64`；
    - `EnvironmentMazes` 默认会被拉到 `128`。

- 这就是为什么我第一次做短时训练 smoke 时：
  - 传了 `target_num_agents=32` 或 `16`；
  - 但训练入口仍然按默认 `agent_bins` 覆盖了 `grid_config.num_agents`；
  - 最终在 `MultiEnv` 里触发了 “`Target num agents must be divisible by num agents`”。

- 现在的使用建议是：
  - 如果继续沿用默认 `agent_bins`，那就让 `target_num_agents` 分别对齐：
    - `Pogema-v0` 用 `64` 的倍数；
    - `PogemaMazes-v0` 用 `128` 的倍数。
  - 如果你想精确控制更小的 agent 数，需要同时修改 `agent_bins`。

## 42. “use_maps=True 仍然不是原生 3D 地图集” 这句话是什么意思

- 这句话的意思是：
  - **训练流程已经能在 2.5D 模式下使用现有地图集；**
  - **但这些地图文件本身还不是三维地图文件。**

- 当前 `use_maps=True` 时，地图来源是 `MAPS_REGISTRY`，也就是 `env/maps.yaml`、`env/random.yaml`、`env/street_map.yaml` 这些现有地图资源。
- 这些地图的内容本质上都是：
  - 一张二维网格；
  - 每个点只有 `(x, y)`；
  - 障碍只有“这一格堵不堵”，没有独立的 `z` 层障碍定义。

- 所以现在 `use_maps=True + height_levels=4` 时，系统做的是：
  - 先读取一张二维地图；
  - 再让智能体带上高度层 `(x, y, z)` 去训练；
  - 连通域、目标、观测、动作已经支持 2.5D；
  - 但底层地图资源本身并没有提供“第 1 层长什么样、第 2 层长什么样、第 3 层哪里有独立障碍”。

- 换句话说：
  - **现在的 mazes / random / street 地图，是“二维地图上飞分层高度”；**
  - **不是“每个高度层都有自己独立障碍结构的三维地图集”。**

- 你可以把它理解成两种完全不同的东西：

- `1. 当前 use_maps=True 的情况`
  - 像是一张仓库平面图，被允许在上面分成 `4` 个飞行高度层；
  - 但这 `4` 层用的还是同一张底图。

- `2. 真正的原生 3D 地图集`
  - 应该是地图文件里直接定义：
    - 第 0 层障碍；
    - 第 1 层障碍；
    - 第 2 层障碍；
    - 第 3 层障碍；
  - 也就是不同高度层可以长得不一样。

- 所以我那句话不是说 `use_maps=True` 现在不能训练。
- 真正的意思是：
  - **它现在已经能训练；**
  - **但训练用的是“二维地图资源 + 分层高度逻辑”，不是“原生 3D 地图资产”。**
