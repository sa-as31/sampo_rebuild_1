# 解疑

这个文档用于持续记录我在项目推进过程中提出的问题，以及当前已经确认的答案。

## 1. 论文中的“多智能体”指什么

- 这里的多智能体，指的是多架无人机。
- 每一架无人机都被看作一个独立智能体，拥有自己的位置、目标、局部观测和动作决策。
- 多智能体路径规划的难点不在单机找路，而在多机共享环境中的冲突规避与协同决策。

## 2. 页面里的“完成比例”是否合理

- 对终身任务来说，这个指标不合适。
- 因为智能体到达目标后不会退出，而是会被分配一个新目标继续执行任务。
- 更适合的指标是：
  - 累计完成任务数
  - 吞吐量

## 3. `max_frames=200` 是否表示每个智能体最多移动 200 步

- 更准确地说，`max_frames` 是整次 Web 推理回放的全局步数上限。
- 在同步多智能体环境里，可以近似理解为每个智能体最多参与 200 次动作决策时刻。
- 实际停止还会受到 `max_episode_steps` 和环境自身结束条件影响。

## 4. 智能体能否感知所有无遮挡区域

- 不能。
- 当前项目里，模型使用的是局部观测，不是“无遮挡全局视野”。
- 经过预处理后，模型主输入是以智能体为中心的 `11 x 11` 局部窗口。
- 因此，窗口外的环境信息对当前一步决策是不可见的。

## 5. 为什么终点就在旁边，智能体还可能在终点附近移动

- 这不一定意味着“没看见终点”。
- 更可能的原因包括：
  - 终身任务设定：到达目标后会立刻分配新目标
  - 多智能体冲突规避：当前一步不一定适合直接进入目标格
  - 奖励设计和子目标跟随机制：模型在局部上可能优先服从 planner 提供的引导
  - 训练效果仍可能不够理想，导致局部绕行或震荡

## 6. 模型在系统里起什么作用

- 模型负责给每个智能体输出下一步动作。
- 动作一般是离散动作，例如上、下、左、右、停留。
- 因此，运行时真正控制智能体移动的是模型的动作输出。

## 7. 智能体移动是否只靠模型

- 不是。
- 这个项目采用的是“规划算法 + 强化学习模型”的两阶段协同方式。
- 可以理解为：
  - A* / LB-A* 类规划器先生成基础路径或子目标引导
  - MARL 模型再结合局部观测和多机交互，对动作做决策精炼
- 因此不是纯模型端到端规划，而是“先规划、再精炼”的协同结构。

## 8. 当前 Web 原型后端是否已经支持终身任务指标

- 已开始改造。
- 当前页面的核心指标已改为：
  - 累计完成任务数
  - 吞吐量（任务数 / 步数）

## 9. 这个项目如何用两句话写进简历

- 可写成：负责基于 Sample Factory 与 Pogema 搭建多无人机协同路径规划与强化学习训练系统，结合传统规划器与多智能体强化学习模型，实现复杂栅格环境下的路径生成、冲突规避与策略训练。
- 可写成：独立完成项目的 CPU/GPU 训练链路、Docker 部署及 Web 推演原型开发，支持模型推理回放、关键指标展示与多智能体动态可视化。

## 10. 运营模式里为什么到终点后又播放了一遍动画

- 原因是前端播放逻辑在到达最后一帧时，会把帧索引重置为 `0`，属于循环播放设计。
- 已修复为：到达最后一帧后自动停止，不再回卷到开头，并将状态显示为“任务已完成”。
- 当前行为：
  - 点击“开始任务”从首帧播放到末帧后停止；
  - 点击“继续”时如果已经在末帧，会保持末帧并提示“任务已完成”。

## 11. 为什么运营模式里会看到无人机穿过灰色墙体

- 你的理解是正确的：灰色块代表障碍/墙体。
- 之前的原因是：运营模式轨迹由前端线性插值生成，没有逐步避障约束。
- 现已替换为栅格 A* 路径生成，样例轨迹会绕开灰色障碍块，不再穿墙。
- 当前状态是：
  - 目标点仍然在前端校验，障碍格不能被设为目标；
  - 轨迹生成从“直线插值动画”升级为“基于障碍地图的可通行路径”。

## 12. 三个任务模板是否有各自对应地图

- 已支持对应关系，不再共用同一障碍布局。
- 当前模板与地图为：
  - 仓储巡检 -> `warehouse-grid-v1`
  - 园区配送 -> `campus-road-v1`
  - 应急调度 -> `emergency-block-v1`
- 在运营模式切换模板时，会重建对应地图、起终点和 A* 轨迹，并从第 0 帧重新展示。

## 13. 未来 3D 模拟页面用哪种方式展示更好

- 推荐方案：`Three.js + glTF + WebSocket`。
- 选择理由：
  - 与当前 Vue 前端集成成本低，能快速做出可交互演示；
  - 适合毕业设计节奏，论文答辩时可展示“模型状态 + 3D轨迹 + 操作面板”的联动；
  - 后续可以平滑接入真实后端推理流（位置、速度、告警）。
- 已在项目中新增“3D仿真预览（演示）”页面作为骨架，后续可把占位渲染替换为真实 Three.js 引擎层。

## 14. 能否单独控制“缓慢环绕视角”的速度

- 已支持。
- 在 3D 仿真页面中，当镜头模式为“环绕观察”时，会显示独立滑杆：`环绕速度 (rad/s)`。
- 该速度与“仿真速度”解耦：
  - 仿真速度控制无人机运动快慢；
  - 环绕速度只控制相机绕场景旋转快慢。
- 页面新增“环绕周期(秒)”指标，方便快速调到答辩演示所需的慢速环绕效果。

## 15. 为什么环绕观察看起来没有绕着地图转

- 原因是相机朝向的 yaw 符号换算有误，导致“位置在绕圈，但画面不呈现正确环绕”。
- 已修复为：按当前投影坐标约定修正 yaw 计算，环绕镜头会持续朝向地图中心并绕地图旋转。
- 同时把默认环绕速度从 `0.08` 调整到 `0.12 rad/s`，更容易直观看到环绕效果。

## 16. 能否把现在的 2D 地图和模型推理先跑在 3D 展示里

- 已实现。
- 3D 页面已接入后端推理接口 `/api/run-demo`，可直接调用当前模型并加载回放数据。
- 渲染逻辑现在使用后端返回的：
  - `environment.obstacles` -> 3D 障碍方块
  - `frames[].agents` -> 3D 无人机位置与目标圈
- 也保留“加载2D示例”入口，便于在后端不可用时继续演示 3D 回放流程。

## 17. 如何把研究模式放到管理员里，并让系统界面更像真实产品

- 已实现“角色入口 + 权限视图”：
  - 右上角新增“执行者 / 管理员”切换按钮；
  - 研究模式仅管理员可见，执行者界面不显示研究入口。
- 页面风格已改为更简洁的控制台样式：
  - 顶部改为紧凑 `Top Bar`；
  - 精简了各页面说明性长文字；
  - 标签和面板尺寸收敛，减少“演示文档感”。

## 18. 3D 界面能否支持放大调节

- 已支持。
- 3D 页面新增“视图放大”滑杆（`x0.60 ~ x2.50`），可实时放大/缩小画面。
- 放大逻辑接入投影焦距参数，回放暂停时调节也会立即重绘生效。

## 19. 鼠标是否可以拖拽 3D 视角

- 已支持。
- 在 3D 画布中按住鼠标左键拖拽即可调整视角：
  - 环绕模式：水平拖拽改变环绕角度，垂直拖拽改变相机高度；
  - 跟随/俯视模式：拖拽可做视角偏航和俯仰微调。
- 拖拽时会暂停自动环绕增量，松开后继续按当前角度播放。

## 20. 能否切换到某个无人机视角并显示它看到的 3D 地图

- 已支持。
- 3D 页面新增“无人机视角”镜头模式，可选择具体无人机编号。
- 启用后会切到该无人机第一视角，并展示其局部 `11 x 11` 范围的 3D 地图内容（障碍、网格、无人机与目标）。
- 同时保留鼠标拖拽微调能力，便于观察该无人机的局部环境。

## 21. 为什么 3D 回放窗口突然空了

- 根因是相机俯仰角限制设置过窄，导致某些模式下相机看向了错误方向，画面看起来像“空白”。
- 已修复：
  - 放宽非无人机模式的俯仰角范围，恢复可见视角；
  - 无人机模式也同步放宽上限，避免边界角度卡死；
  - 新增“无可绘制地图数据”提示，防止再次出现黑屏无反馈。

## 22. 无人机视角里方块能否改成实体不透明，是否还有其他 bug

- 已改为实体不透明：
  - 在“无人机视角”模式下，障碍块从线框改为实体立方体（带顶面和侧面填充）；
  - 其他镜头模式继续保持线框，兼顾全局观察清晰度。
- 额外检查并修复：
  - 调整了无人机视角拖拽时的俯仰限制，避免过冲导致画面异常。
- 快速检查结果：
  - 前端构建通过（`npm --prefix web_frontend run build`）；
  - 当前未发现新的编译期错误。
